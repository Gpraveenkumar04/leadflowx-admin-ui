// Import the compiled TypeScript implementation explicitly to avoid resolving back to this .jsx file.
import GoogleMapsScraperStatus from './GoogleMapsScraperStatus.tsx';

// Re-export the TypeScript implementation to satisfy consumers that import the .jsx path.
export default GoogleMapsScraperStatus;
